require 'spec_helper'

describe Functional do
  it 'has a version number' do
    expect(Functional::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
